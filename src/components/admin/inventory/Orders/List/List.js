
import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`

`;

const List = (props) => {
    return (
        <Wrapper>

        </Wrapper>
    );
}

export default List;