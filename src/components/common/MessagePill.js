
import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
    font-size: 10pt;
    
`;

function MessagePill({ msg, appearance }) {
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default MessagePill;